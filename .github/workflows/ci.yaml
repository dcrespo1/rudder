name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  # ─── Unit tests + coverage ────────────────────────────────────────────────
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      - name: Run tests
        run: go test ./... -timeout 60s -coverprofile=coverage.out

      - name: Coverage summary
        run: go tool cover -func=coverage.out | tail -1

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-${{ github.sha }}
          path: coverage.out
          retention-days: 7

  # ─── Lint ───────────────────────────────────────────────────────────────── NEEDS WORK
  # lint:
  #   name: Lint
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4

  #     - uses: actions/setup-go@v6
  #       with:
  #         go-version-file: go.mod
  #         cache: true

  #     - name: golangci-lint
  #       uses: golangci/golangci-lint-action@v7
  #       with:
  #         version: latest
  #         args: --timeout 5m

  # ─── Vulnerability check ──────────────────────────────────────────────────
  # vulncheck:
  #   name: Vuln Check
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4

  #     - uses: actions/setup-go@v6
  #       with:
  #         go-version-file: go.mod
  #         cache: true

  #     - name: Install govulncheck
  #       run: go install golang.org/x/vuln/cmd/govulncheck@latest

  #     - name: Run govulncheck
  #       run: govulncheck ./...

  # ─── Snapshot build (non-tag pushes) ─────────────────────────────────────
  build-snapshot:
    name: Snapshot Build
    runs-on: ubuntu-latest
    needs: [test] # removing lint for now
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      # - name: Install cosign
      #   uses: sigstore/cosign-installer@v3

      - uses: goreleaser/goreleaser-action@v7
        with:
          version: "~> v2"
          args: release --snapshot --clean

      - name: Upload snapshot binaries
        uses: actions/upload-artifact@v4
        with:
          name: rudder-snapshot-${{ github.sha }}
          path: dist/
          retention-days: 3
